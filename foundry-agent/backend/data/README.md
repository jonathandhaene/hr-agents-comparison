# Backend data

The contents of this folder are **copied from `/shared-fixtures/`** at build time by the solution's `Makefile` / CI pipeline. Do not edit files here directly — change the source under `/shared-fixtures/` and re-run `make seed`.
